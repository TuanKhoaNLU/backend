server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quiz_web_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Kocopass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: change-this-secret-key-to-a-long-random-value
    expiration-ms: 86400000
